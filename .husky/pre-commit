#!/usr/bin/env sh
# Temporarily disable pre-commit test execution.
# This hook exits successfully immediately so local/CI pre-commit hooks that
# delegate to Husky won't run the project's test suite and block commits.
#
# If you want to re-enable hooks later, replace this file or remove it.
exit 0