# Package initializer for routers package.
# This file makes pdf-ingest/routers a proper Python package so imports like
# `from routers.full_raw_text import router` work when pdf-ingest is on sys.path.
