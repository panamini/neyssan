import { describe } from "vitest";

describe.skip("useCvParser.test.ts (placeholder)", () => {
  // This file intentionally contains a skipped placeholder to avoid Vitest
  // reporting "No test suite found" when the real tests are in the .tsx file.
});